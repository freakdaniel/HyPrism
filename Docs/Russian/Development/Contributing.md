# Руководство по контрибьюции

Мы приветствуем вклад сообщества! Будь то исправление багов, улучшение документации или добавление новых функций — ваша помощь ценна.

---

## 🛠️ Требования для разработки

| Инструмент | Требование |
|------------|------------|
| **IDE** | Visual Studio 2022, Rider, или VS Code |
| **SDK** | .NET 10.0 |
| **Git** | Базовые знания git-операций |
| **OS** | Windows 10+, Linux (Ubuntu 22.04+), macOS 12+ |

---

## 📋 Рабочий процесс

### 1. Fork репозитория

Нажмите "Fork" на GitHub для создания вашей копии.

### 2. Клонирование

```bash
git clone https://github.com/<YOUR_USERNAME>/HyPrism.git
cd HyPrism
```

### 3. Стратегия ветвления

> ⚠️ Никогда не коммитьте напрямую в `master`/`main`!

**Конвенция именования:** `type/short-description`

| Тип | Пример |
|-----|--------|
| Новая функция | `feat/mod-manager-ui` |
| Исправление | `fix/crash-on-startup` |
| Документация | `docs/update-readme` |
| Рефакторинг | `refactor/split-appservice` |
| Стилизация | `style/button-animations` |

```bash
git checkout -b feat/my-new-feature
```

### 4. Разработка

1. Внесите изменения
2. Убедитесь, что код компилируется: `dotnet build`
3. Проверьте наличие ошибок: `dotnet run`
4. Следуйте [CodingStandards.md](CodingStandards.md)

### 5. Коммит

```bash
git add .
git commit -m "feat: добавлен менеджер модов"
```

**Формат коммитов:**
- `feat:` — новая функция
- `fix:` — исправление бага
- `docs:` — документация
- `refactor:` — рефакторинг
- `style:` — форматирование/стили
- `chore:` — прочие изменения

Ссылайтесь на Issues: `fix: ошибка авторизации (#123)`

### 6. Push и Pull Request

```bash
git push origin feat/my-new-feature
```

Откройте PR на официальный репозиторий и заполните шаблон.

---

## 📐 Правила кода

### MVVM

- ViewModels **не** должны ссылаться на Avalonia Controls
- Используйте `RaiseAndSetIfChanged` и `ReactiveCommand`
- Получайте сервисы через DI

### Стили

- Используйте `StaticResource`/`DynamicResource`
- SVG для иконок (не Bitmap)
- Следуйте [StylingGuide.md](StylingGuide.md)

### Тесты

- Покрывайте критические сервисы тестами
- Используйте моки для зависимостей

---

## 🐛 Сообщение об ошибках

Если нашли баг, но не можете исправить:

1. Перейдите во вкладку **Issues**
2. Проверьте, не существует ли уже такой issue
3. Создайте новый Issue с:
   - Логами (`Logs/*.log`)
   - Скриншотами (для визуальных багов)
   - Шагами для воспроизведения
   - Версией ОС и .NET

---

## 📁 Структура для новых компонентов

При создании нового UI компонента:

```
UI/Components/<Category>/<ComponentName>/
├── <ComponentName>.axaml
├── <ComponentName>.axaml.cs
└── <ComponentName>ViewModel.cs  (опционально)
```

См. [UIComponentGuide.md](UIComponentGuide.md).

---

## 🔍 Code Review

При ревью PR мы проверяем:

- [ ] Код компилируется без warnings
- [ ] Соблюдены стандарты кодирования
- [ ] MVVM паттерн соблюдён
- [ ] Нет hardcoded значений
- [ ] Документация обновлена (при необходимости)

---

## 📚 Полезные ссылки

- [CodingStandards.md](CodingStandards.md) — Стандарты кода
- [MVVMPatterns.md](MVVMPatterns.md) — Паттерны MVVM
- [UIComponentGuide.md](UIComponentGuide.md) — Создание компонентов
- [Architecture.md](../Technical/Architecture.md) — Архитектура
