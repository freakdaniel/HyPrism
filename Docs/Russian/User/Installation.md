# Руководство по установке

## Системные требования

| | Минимальные | Рекомендуемые |
|---|---|---|
| **ОС** | Windows 10, Linux (Modern), macOS 12 | Windows 11, Ubuntu 22.04 LTS, macOS 14 |
| **RAM** | 2 GB | 4 GB |
| **Диск** | 200 MB (только лаунчер) | 10 GB+ (лаунчер + игра) |
| **Графика** | DirectX 10 / OpenGL 3.3 | DirectX 11+ / Vulkan |
| **.NET** | .NET 10 Runtime | .NET 10 SDK (для сборки) |

---

## 🪟 Установка на Windows

### Вариант A: Установщик

1. **Скачайте** `HyPrism-Setup.exe` со [страницы релизов](https://github.com/yyyumeniku/HyPrism/releases)
2. **Запустите** установщик
   > Windows SmartScreen может предупредить о неподписанном файле — нажмите "Всё равно запустить"
3. **Готово!** Ярлык появится на рабочем столе

### Вариант B: Портативная версия

1. Скачайте `HyPrism-win-x64.zip`
2. Распакуйте в любую папку
3. Запустите `HyPrism.exe`

---

## 🐧 Установка на Linux

### Вариант A: Flatpak (рекомендуется)

Flatpak обеспечивает изоляцию и все зависимости в комплекте.

```bash
# Добавление Flathub (если ещё не добавлен)
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

# Установка HyPrism
flatpak install dev.hyprism.HyPrism
```

### Вариант B: Портативный бинарник

```bash
# Скачивание и распаковка
wget https://github.com/yyyumeniku/HyPrism/releases/download/latest/HyPrism-linux-x64.tar.gz
tar -xzf HyPrism-linux-x64.tar.gz
cd HyPrism

# Установка прав и запуск
chmod +x HyPrism
./HyPrism
```

### Возможные зависимости

В зависимости от дистрибутива может потребоваться:

```bash
# Ubuntu/Debian
sudo apt install libicu-dev libssl-dev libfontconfig1

# Fedora
sudo dnf install icu libssl fontconfig

# Arch
sudo pacman -S icu openssl fontconfig
```

### Проблема с .NET версией

Если возникает ошибка версии .NET:

```bash
export DOTNET_ROLL_FORWARD=Major
./HyPrism
```

---

## 🍎 Установка на macOS

### Шаги установки

1. **Скачайте** `HyPrism.dmg`
2. **Смонтируйте** — двойной клик по `.dmg`
3. **Установите** — перетащите иконку HyPrism в папку Applications

### Предупреждение безопасности

При первом запуске macOS может заблокировать приложение от "Неизвестного разработчика":

1. Откройте **Системные настройки** → **Конфиденциальность и безопасность**
2. Прокрутите вниз и нажмите **"Всё равно открыть"** для HyPrism

---

## 🔧 Сборка из исходников

### Требования

- Git
- .NET 10 SDK

### Шаги

```bash
# Клонирование
git clone https://github.com/yyyumeniku/HyPrism.git
cd HyPrism

# Сборка
dotnet build

# Запуск
dotnet run
```

### Публикация

```bash
# Self-contained для Linux
dotnet publish -c Release -r linux-x64 --self-contained

# Self-contained для Windows
dotnet publish -c Release -r win-x64 --self-contained

# Self-contained для macOS
dotnet publish -c Release -r osx-x64 --self-contained
```

---

## 📁 Расположение данных

После установки HyPrism создаёт папку с данными:

| ОС | Путь |
|----|------|
| Windows | `%APPDATA%\HyPrism\` |
| Linux | `~/.config/HyPrism/` |
| macOS | `~/Library/Application Support/HyPrism/` |

Содержимое:
- `config.json` — настройки
- `profiles/` — профили пользователей
- `Instances/` — установленные версии игры
- `Logs/` — логи

---

## ❓ Устранение неполадок

### Лаунчер не запускается

1. Проверьте установку .NET 10 Runtime
2. Проверьте логи в папке `Logs/`
3. Попробуйте `DOTNET_ROLL_FORWARD=Major`

### Белый/чёрный экран

Возможно, проблема с графическим драйвером:
```bash
# Принудительно использовать software rendering
export AVALONIA_RENDER=software
./HyPrism
```

### Проблемы с загрузкой игры

Проверьте:
- Подключение к интернету
- Свободное место на диске
- Права на папку Instances

---

## 📚 Дополнительные ресурсы

- [Configuration.md](Configuration.md) — Настройка конфигурации
- [Features.md](../General/Features.md) — Описание функций
